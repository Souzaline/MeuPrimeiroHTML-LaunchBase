{% extends "layout.njk" %} 

{% block head %}

    <title>{{ item.title }}</title>
    
{% endblock %} 

{% block content %}

    <div class="wrapper">
    <h1>{{ item.title }}</h1>
    </div>

    <section class="cursos">
        <div class="curso" id="{{ item.id }}">
            <div class="card-curso-container">
                <iframe src="{{item.id}}" frameborder="0"></iframe>
            </div>
            
        </div>

    </section>



{% endblock %}
